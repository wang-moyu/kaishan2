# 本机 Node 22.15.1 自带的 npm 10.9.2 在解析 vitest 4.1.x 的 peer 集合时会崩溃：
#   TypeError: Cannot read properties of null (reading 'edgesOut')
#   at @npmcli/arborist/lib/arborist/build-ideal-tree.js #loadPeerSet
# 同一依赖组合用 npm 11.19.1 实测可正常解析，因此这是 npm 10 的解析缺陷，不是依赖冲突。
# vitest 4.1.x 是 @cloudflare/vitest-pool-workers（workerd 内运行测试）的 peer 要求，不可替换。
# 该开关只让 npm 10 跳过自动 peer 安装；本项目需要的 peer（vue、eslint、vite 等）均已显式声明。
# 升级到 npm >= 11 后可删除本文件。
legacy-peer-deps=true
