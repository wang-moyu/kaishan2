{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // cloudflare:test 的模块声明由该包的 types 入口提供
    "types": ["@cloudflare/vitest-pool-workers/types"]
  },
  "include": [
    "src/**/*.ts",
    "test/**/*.ts",
    "vitest.config.ts",
    "worker-configuration.d.ts"
  ]
}
